spring:
  application:
    name: Workly

  datasource:
    url: jdbc:mysql://localhost:5432/worklydb
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
      format_sql: true